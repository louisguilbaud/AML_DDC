<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Suivi Navire Segmenté - SAS VA</title>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body { 
            margin: 0; 
            padding: 0; 
            font-family: 'Inter', sans-serif; 
            background: #f1f5f9; /* Fond clair de secours */
        }
        
        #map { 
            height: 100vh; 
            width: 100vw; 
            background: #f1f5f9; 
        }

        /* --- Style de la Popup : Version Claire Épurée --- */
        .leaflet-popup-content-wrapper {
            background: rgba(255, 255, 255, 0.85) !important; /* Blanc semi-transparent */
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 0, 0, 0.06); 
            border-radius: 14px !important;
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1) !important;
            color: #1e293b !important; /* Texte sombre pour le contraste */
            padding: 6px;
        }

        .leaflet-popup-tip {
            background: rgba(255, 255, 255, 0.85) !important;
            border: 1px solid rgba(0, 0, 0, 0.06);
        }

        .leaflet-popup-close-button {
            color: #94a3b8 !important;
            padding: 10px 10px 0 0 !important;
        }

        .info-box { 
            font-size: 13px; 
            min-width: 250px; 
            line-height: 1.6;
        }
        
        .header-container {
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-bottom: 1px solid rgba(0, 0, 0, 0.06); 
            margin-bottom: 12px; 
            padding-bottom: 8px; 
        }

        .header-title { 
            font-weight: 700; 
            color: #0284c7; /* Bleu océan plus soutenu pour fond clair */
            text-transform: uppercase; 
            letter-spacing: 0.7px;
            font-size: 13px;
        }

        .status-badge {
            display: inline-flex;
            align-items: center;
            background: rgba(22, 163, 74, 0.1);
            color: #16a34a;
            padding: 3px 9px;
            border-radius: 20px;
            font-size: 10px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .status-dot {
            width: 6px;
            height: 6px;
            background-color: #16a34a;
            border-radius: 50%;
            margin-right: 5px;
            display: inline-block;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 6px;
        }

        .info-label {
            color: #64748b; /* Gris intermédiaire */
            font-weight: 500;
        }

        .info-value {
            color: #0f172a; /* Texte important très sombre */
            font-weight: 600;
        }

        .info-footer {
            border-top: 1px solid rgba(0, 0, 0, 0.04);
            margin-top: 10px;
            padding-top: 8px;
            color: #64748b;
            font-size: 11px;
            text-align: center;
            font-family: monospace;
        }

        /* --- Animation Pulse Icône (Adaptée pour le fond clair) --- */
        @keyframes pulse {
            0% { filter: drop-shadow(0 0 2px rgba(22, 163, 74, 0.4)); }
            50% { filter: drop-shadow(0 0 8px rgba(22, 163, 74, 0.7)); }
            100% { filter: drop-shadow(0 0 2px rgba(22, 163, 74, 0.4)); }
        }
        .vessel-pulse {
            animation: pulse 2.5s infinite ease-in-out;
        }
    </style>
</head>
<body>

    <div id="map"></div>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/leaflet-rotatedmarker@0.2.0/leaflet.rotatedMarker.min.js"></script>

    <script>
        const map = L.map('map', { 
            minZoom: 2,
            maxZoom: 18,
            bounceAtZoomLimits: false
        }).setView([0, 0], 2);
        
        // Changement pour un fond "Positron" (CartoDB Light) ultra-épuré et clair
        L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
            attribution: '&copy; <a href="https://carto.com/">CARTO</a> &copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>',
            subdomains: 'abcd',
            maxZoom: 20
        }).addTo(map);

        let pathLayer = L.featureGroup().addTo(map);

        function onMessage(event) {
            if (event.data && event.data.data) {
                renderDataTrail(event.data.data);
            }
        }

        function renderDataTrail(rows) {
            pathLayer.clearLayers();
            if (rows.length === 0) return;

            let latestIndex = 0;
            let maxTimestamp = -1;

            rows.forEach((row, index) => {
                const ts = new Date(row[8]).getTime();
                if (ts > maxTimestamp) {
                    maxTimestamp = ts;
                    latestIndex = index;
                }
            });

            const allCoordinates = [];
            let currentPath = [];
            const threshold = 10; 

            rows.forEach((row, index) => {
                const lat = row[0];
                const lng = row[1];
                const heading = row[2];

                if (lat == null || lng == null) return;

                if (currentPath.length > 0) {
                    const lastPoint = currentPath[currentPath.length - 1];
                    const distance = Math.sqrt(Math.pow(lat - lastPoint[0], 2) + Math.pow(lng - lastPoint[1], 2));
                    
                    if (distance > threshold) {
                        allCoordinates.push(currentPath);
                        currentPath = [];
                    }
                }
                
                currentPath.push([lat, lng]);

                if (index === latestIndex) {
                    // Icône d'un vert un peu plus soutenu (#16a34a) pour trancher sur le blanc
                    const shipIcon = L.divIcon({
                        html: `<svg class="vessel-pulse" viewBox="0 0 24 24" width="42" height="42" fill="#16a34a">
                                <path d="M12,2L4.5,20.29L5.21,21L12,18L18.79,21L19.5,20.29L12,2Z"/></svg>`,
                        className: '',
                        iconSize: [42, 42],
                        iconAnchor: [21, 21]
                    });

                    let dateFormatee = row[8];
                    try {
                        const d = new Date(row[8]);
                        if(!isNaN(d)) {
                            dateFormatee = d.toLocaleString('fr-FR', { timeZone: 'UTC' }) + ' UTC';
                        }
                    } catch(e) {}

                    const marker = L.marker([lat, lng], { 
                        icon: shipIcon,
                        rotationAngle: heading 
                    }).addTo(pathLayer);

                    marker.bindPopup(`
                        <div class="info-box">
                            <div class="header-container">
                                <div class="header-title">${row[5] || 'NAVIRE'}</div>
                                <div class="status-badge"><span class="status-dot"></span>LIVE</div>
                            </div>
                            <div class="info-row"><span class="info-label">Date</span><span class="info-value">${dateFormatee}</span></div>
                            <div class="info-row"><span class="info-label">Vitesse</span><span class="info-value">${row[3]?.toFixed(1)} kts</span></div>
                            <div class="info-row"><span class="info-label">Cap</span><span class="info-value">${heading}°</span></div>
                            <div class="info-row"><span class="info-label">Tonnage</span><span class="info-value">${row[6] ? row[6].toLocaleString() + ' DWT' : 'N/A'}</span></div>
                            <div class="info-footer">POS: ${lat.toFixed(5)}, ${lng.toFixed(5)}</div>
                        </div>
                    `).openPopup();
                }
            });
            
            allCoordinates.push(currentPath);

            // Dessin du sillage bleu cyan soutenu (très visible sur fond blanc)
            allCoordinates.forEach(path => {
                if (path.length > 1) {
                    L.polyline(path, {
                        color: '#0284c7', // Bleu plus foncé pour le contraste clair
                        weight: 4,
                        opacity: 0.2,
                        lineJoin: 'round'
                    }).addTo(pathLayer);

                    L.polyline(path, {
                        color: '#0ea5e9', // Ligne centrale vive
                        weight: 2.5,
                        opacity: 0.9,
                        lineJoin: 'round',
                        dashArray: '6, 5'
                    }).addTo(pathLayer);
                }
            });

            if (pathLayer.getBounds().isValid()) {
                map.fitBounds(pathLayer.getBounds(), { padding: [60, 60] });
            }
        }

        window.addEventListener('message', onMessage, false);
    </script>
</body>
</html>
